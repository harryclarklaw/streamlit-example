{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Horizon Engine Configuration",
  "description": "User configuration for sector-focused regulatory and policy monitoring",
  "type": "object",
  "required": ["sectors", "geographies", "clientTypes", "strategicPriorities"],
  "properties": {
    "sectors": {
      "type": "array",
      "description": "Industry sectors to monitor (e.g., 'interactive entertainment', 'fintech', 'life sciences')",
      "items": { "type": "string", "minLength": 1 },
      "minItems": 1
    },
    "geographies": {
      "type": "array",
      "description": "Geographic markets to monitor (e.g., 'UK', 'EU', 'Singapore', 'US')",
      "items": { "type": "string", "minLength": 1 },
      "minItems": 1
    },
    "clientTypes": {
      "type": "array",
      "description": "Types of clients or organisations to focus on (e.g., 'mobile games developers', 'crypto exchanges')",
      "items": { "type": "string", "minLength": 1 },
      "minItems": 1
    },
    "strategicPriorities": {
      "type": "array",
      "description": "Specific regulatory or policy topics of particular interest (e.g., 'loot box regulation', 'AI governance')",
      "items": { "type": "string", "minLength": 1 },
      "minItems": 1
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when the configuration was created"
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp when the configuration was last updated"
    }
  },
  "additionalProperties": false
}
